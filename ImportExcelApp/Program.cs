// For IIS Web API hosting, this class is not used as HTTP entrypoint.
// Lifecycle is managed by Global.asax / Application_Start.
public class Program
{
    public static void Main(string[] args)
    {
        // No-op for IIS. Keep for optional local standalone tests if needed.
    }
}
